# Reviewing f-strings and how they make string interpolation simpler
val = 'Silly'
print(f"{val} was feeling {val} throughout the performance.")

# f-strings are faster than % formatting and str.format()
name = 'Isaac'
location = 'Paris'
age = 10
print(f"{name} couldn't believe he'd made it to {location} at {age}!")

# backslash can't be used correctly
f"newline: {ord(\n')}" #results in error

# put backslash into a variable as a workaround
newline = ord('\n')
print(f"newline: {newline}")
